#include<cmath>

using namespace std;

constexpr double pi = 3.14; //contexpr tel the compiler it can be resolved at compile time, 
                            //cannot not use return values for initialization
const double pi = atan(1.0);

int main() {


}